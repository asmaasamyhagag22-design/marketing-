"""Stage 1 extractors.

Each module here takes inputs from the fetcher (HTML, screenshot,
or live Page) and produces structured data validated against the
schemas defined in `scraper.schemas`. Nothing here writes to disk.
"""
