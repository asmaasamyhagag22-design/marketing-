# CLAUDE.md

**Read [process.md](process.md) first — it is the single source of truth** for this
project: vision, standing rules, runtime/model map, full feature inventory, honest
gaps, the ordered roadmap, and the measured lessons.

This file exists only because the assistant auto-loads `CLAUDE.md` by name. Keep all
project knowledge in `process.md` (and update it as part of every landed change —
rule 8). The old CLAUDE.md change-log lives in git history if ever needed.
